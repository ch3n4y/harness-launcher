{
  "name": "harness-launcher",
  "version": "0.1.1",
  "description": "DeepSeek Harness service launcher",
  "author": "Harness Launcher contributors",
  "license": "MIT",
  "scripts": {
    "start": "tauri dev",
    "test": "node scripts/test-renderer.cjs && cargo test --manifest-path src-tauri/Cargo.toml",
    "build": "tauri build",
    "dist:win": "tauri build --bundles nsis",
    "dist:mac": "tauri build --target universal-apple-darwin --bundles app,dmg"
  },
  "devDependencies": {
    "@tauri-apps/cli": "2.11.4"
  }
}
